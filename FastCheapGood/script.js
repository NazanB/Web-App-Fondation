"use strict";
/*
let lastActivitySwitch = null;

function tasteToggle() {
  const fastCheckbox = document.getElementById("fast");
  const cheapCheckbox = document.getElementById("cheap");
  const goodCheckbox = document.getElementById("good");

  fastCheckbox.addEventListener("click", () => {
    if (fastCheckbox.checked) {
      lastActivitySwitch = "fast";
      cheapCheckbox.checked = false;
      goodCheckbox.checked = false;
    }
  });

  cheapCheckbox.addEventListener("click", () => {
    if (cheapCheckbox.checked) {
      lastActivitySwitch = "cheap";
      fastCheckbox.checked = false;
      goodCheckbox.checked = false;
    }
  });

  goodCheckbox.addEventListener("click", () => {
    if (goodCheckbox.checked) {
      lastActivitySwitch = "good";
      fastCheckbox.checked = false;
      cheapCheckbox.checked = false;
    }
  });
}
*/
